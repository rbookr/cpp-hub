
```plaintext

    return std::accumulate(
               scores.cbegin(), scores.cend(),
               0
            ) / (double) scores.size();

//所有的元素的乘积
    return std::accumulate(
               scores.cbegin(), scores.cend(),
               1,
               std::multiplies<int>()
            );
```


