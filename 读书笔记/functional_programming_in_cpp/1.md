

`std::cout`的使用

```cpp
return std::count(
    std::istream_iterator<char>(in),
    std::istream_iterator<char>(),
    '\n');
```

```plaintext
    std::transform(files.cbegin(), files.cend(),
                   results.begin(),
                   count_lines);
```
